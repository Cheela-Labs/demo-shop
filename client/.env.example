# Browser-visible config. Anything here is compiled into the JavaScript the
# visitor downloads, so only ever put PUBLIC values in this file.

# The runtime's PUBLIC key (ch_pk_...). Safe to embed by design.
# Leave unset and the shopping assistant simply does not render.
VITE_CHEELA_PUBLIC_KEY=ch_pk_...
